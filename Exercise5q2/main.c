#include "Complex.c"
#include <stdio.h>

Complex getComplexNum(a, b)
{
         printf("Enter number");
         Complex temp;
         scanf("%l    
         

enum Operations
{
	ADD = 1, SUB, MULT, DIV, EXIT
};

int main()
{
	int input;

	do
	{
		switch (input)
		{
                case EXIT:
			break;

		case ADD:
                     
                        break;
		case SUB:
			break;
		case MULT:
			break;
		case DIV:
			break;
		
		default:
			printf("Number entered unrecognized");
		}

	} while (input != 5)


}
