#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#ifndef _CHECKID_H_
#define _CHECKID_H_

/// @brief Checks the id and tells whether it is valid or invalid based on input
/// @param id number to check
bool CheckID(int number);

#endif